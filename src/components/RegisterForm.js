import { Button } from "react-bootstrap";

const RegisterForm = () => {
    const labelClasses = "px-2 my-1 mx-3";

    return (
        <div>
            <h1>Register here!</h1>
        </div>
    );
};

export default RegisterForm;
